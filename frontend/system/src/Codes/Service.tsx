import React from 'react';
import { Routes, Route } from '@uc-frontend/common';

const CodesService = () => {
  return (
    <Routes>
      {/* <Route path="/" element={<ListView />} /> */}
      <Route path="/" element={<>HELLOOOO</>} />
    </Routes>
  );
};

export default CodesService;
