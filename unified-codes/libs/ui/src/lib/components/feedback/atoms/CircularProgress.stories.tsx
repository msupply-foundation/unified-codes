import React from 'react';
import { CircularProgress } from './CircularProgress';

export default {
  component: CircularProgress,
  title: 'CircularProgress',
};

export const withNoProps = () => <CircularProgress />;
