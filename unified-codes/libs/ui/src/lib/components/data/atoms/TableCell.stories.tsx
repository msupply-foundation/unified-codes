import React from 'react';
import { TableCell } from './TableCell';

export default {
  component: TableCell,
  title: 'Table/TableCell',
};

export const withNoProps = () => {
  return <TableCell />;
};
