import React from 'react';
import { TableBody } from './TableBody';

export default {
  component: TableBody,
  title: 'Table/TableBody',
};

export const withNoProps = () => {
  return <TableBody />;
};
