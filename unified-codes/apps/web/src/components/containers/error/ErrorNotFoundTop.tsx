import * as React from 'react';

import { Typography } from '@unified-codes/ui/components';

export const ErrorNotFoundTop = () => <Typography variant="h3">Nothing to see here...</Typography>;

export default ErrorNotFoundTop;
