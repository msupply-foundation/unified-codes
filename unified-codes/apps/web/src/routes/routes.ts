import { DetailPage, ExplorerPage, ErrorPage, LoginPage } from '../components';

export const ROUTES = {
    DETAIL: DetailPage,
    EXPLORER: ExplorerPage,
    ERROR: ErrorPage,
    LOGIN: LoginPage,
};

export default ROUTES;