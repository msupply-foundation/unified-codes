type Entity {
  code
  description
  type
  has_property
  has_child
}

type Property {
  type
  value
}

code: string @index(exact, fulltext).
description: string @index(exact, fulltext) .
type: string @index(exact) .
value: string .
has_property: [uid] .
has_child: [uid] .